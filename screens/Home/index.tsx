import { ScrollView } from "react-native";

// local imports
import Post from "../../components/Post";

const Home = () => {
  return (
    <ScrollView>
      <Post />
    </ScrollView>
  );
};

export default Home;
